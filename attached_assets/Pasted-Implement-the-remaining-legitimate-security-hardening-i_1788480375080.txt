Implement the remaining legitimate security hardening items from the Jersey Quik Fix audit.

Preserve all existing site functionality, design, products, customer data, repair data, authentication, Stripe checkout, Cloudflare deployment, and admin functionality.

Fix these items:

SPA fallback behavior
Return real 404 responses for dotfiles, nonexistent source maps, executable-looking extensions, debug paths, preview paths, and missing asset-like requests.
Do not return index.html for paths such as /.env, nonexistent .js, .map, .php, .exe, .sh, .bat, etc.
Preserve normal React SPA routing for legitimate application routes.
Admin login brute-force protection
Add per-IP rate limiting.
Add exponential backoff or temporary lockout.
Limit request body size.
Add Cloudflare-compatible protection.
Do not change the current admin password or authentication flow unless necessary.
Repair lookup security
Replace six-digit Math.random() ticket codes with cryptographically secure opaque identifiers for newly created repairs.
Rate-limit repair lookup attempts.
Preserve existing repair codes so current customers do not lose access.
Add a second verification value only if it can be implemented without breaking existing repair records.
Public form abuse protection
Add field length limits and schema validation.
Add per-IP rate limiting.
Add bot/abuse protection where appropriate.
Do not interfere with legitimate customers submitting repairs or trade-ins.
External product image URLs
Prevent arbitrary unsafe image hosts.
Prefer images uploaded to the Jersey Quik Fix image bucket.
If external URLs must remain supported, allowlist only approved HTTPS hosts.
Do not break currently valid product images.
R2 image validation
Inventory existing R2 product images.
Verify image magic bytes and MIME types.
Allow only JPEG, PNG and WebP through the public image route.
Do not delete legitimate images.
Dependencies
Upgrade vulnerable production-relevant dependencies using compatible versions.
Specifically address the qs advisory through a compatible Stripe dependency upgrade rather than a forced override.
Do not blindly force major-version upgrades.
Do not break the production build.
Security headers
Keep the currently deployed CSP, HSTS, nosniff, DENY framing, referrer policy and permissions policy.
Tighten CSP where safely possible.
Do not remove required Clerk or Stripe functionality.
Clerk
Do not migrate the Clerk development instance yet unless you can guarantee preservation of all existing user identities and login access.
Provide a migration plan separately.

After implementing the fixes:

run the complete production build
run security/dependency scans
crawl the live deployed website
verify Stripe checkout
verify Clerk authentication
verify admin login
verify repair lookup
verify product images
verify all legitimate SPA routes
confirm malicious-looking nonexistent paths return proper 404s

Deploy only after all checks pass.

Then give me a final before/after security report listing every change and confirming whether anything remains that could plausibly trigger Sophos, Fortinet or alphaMountain.