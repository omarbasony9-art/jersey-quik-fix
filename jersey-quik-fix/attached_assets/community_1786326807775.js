
document.addEventListener("DOMContentLoaded",()=>{const d=jqLoad();cEvents.textContent=d.events.length;cAnnouncements.textContent=d.announcements.length;cActions.textContent=d.actions.length;d.announcements.forEach(a=>{const e=document.createElement("article");e.className="jq-card";e.innerHTML=`<span class="pill">${a.category}</span><h3>${a.title}</h3><p>${a.text}</p>`;announcementGrid.appendChild(e)});d.events.forEach(v=>{const e=document.createElement("article");e.className="jq-card event-card";e.innerHTML=`<div><span class="pill">${v.date}</span><h3>${v.title}</h3><p>📍 ${v.location}</p></div><strong>${v.time}</strong>`;eventGrid.appendChild(e)});d.actions.forEach(a=>{const e=document.createElement("article");e.className="jq-card";e.innerHTML=`<span class="pill">${a.status}</span><h3>${a.title}</h3><p>${a.text}</p><div class="progress-track"><i style="width:${a.progress}%"></i></div><small>${a.participants} participants · ${a.progress}% complete</small>`;actionGrid.appendChild(e)})})
